apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apple-be.fullname" . }}-test-connection"
  labels:
    {{- include "apple-be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apple-be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
